# Game Chat System Architecture

## High-Level Flow

```
┌─────────────────────────────────────────────────────────────────┐
│                     ANY STREAM PAGE                             │
│  /watch/ORG/TEAM  |  /direct/slug  |  /game/:gameId            │
└────────────────────────┬────────────────────────────────────────┘
                         │
                         │ 1. Get gameId from bootstrap
                         ▼
        ┌────────────────────────────────────┐
        │   useViewerIdentity({ gameId })    │
        │   - Manages unlock state           │
        │   - Persists to localStorage       │
        │   - Issues JWT token               │
        └────────────┬───────────────────────┘
                     │
          ┌──────────┴──────────┐
          │                     │
    NOT UNLOCKED          UNLOCKED
          │                     │
          ▼                     ▼
┌─────────────────────┐  ┌──────────────────────┐
│ ViewerUnlockForm    │  │ useGameChat()        │
│ - Email/Name input  │  │ - SSE connection     │
│ - Calls unlock API  │  │ - Message sending    │
│ - Returns token     │  │ - Auto-reconnect     │
└─────────────────────┘  └──────────┬───────────┘
                                    │
                                    ▼
                         ┌──────────────────────┐
                         │   GameChatPanel      │
                         │   - Message list     │
                         │   - Composer         │
                         │   - Char counter     │
                         │   - Error handling   │
                         └──────────────────────┘
```

---

## Backend Architecture

```
┌─────────────────────────────────────────────────────────────────┐
│                         API LAYER                               │
└─────────────────────────────────────────────────────────────────┘

POST /api/public/games/:gameId/viewer/unlock
  ↓
  ├─ Verify game exists
  ├─ Upsert ViewerIdentity (email, firstName, lastName)
  ├─ Generate JWT (viewerId, gameId, displayName, 24h expiry)
  └─ Return { viewerToken, viewer, gameId }

────────────────────────────────────────────────────────────────────

POST /api/public/games/:gameId/chat/messages
  ↓
  ├─ Verify JWT token (requireViewerAuth middleware)
  ├─ Verify gameId matches token (requireGameMatch middleware)
  ├─ Validate message (1-240 chars)
  ├─ Save to DB (ChatRepository)
  ├─ Broadcast via PubSub
  └─ Return message

────────────────────────────────────────────────────────────────────

GET /api/public/games/:gameId/chat/stream?token=<jwt>
  ↓
  ├─ Verify JWT token
  ├─ Send initial snapshot (last 50 messages, newest-first)
  ├─ Subscribe to game's pubsub channel
  ├─ Stream new messages via SSE
  ├─ Keep-alive ping every 30s
  └─ Cleanup on disconnect

────────────────────────────────────────────────────────────────────

┌─────────────────────────────────────────────────────────────────┐
│                       SERVICE LAYER (ISP)                        │
└─────────────────────────────────────────────────────────────────┘

ChatService
  ├─ sendMessage(input)
  │   ├─ Validate (trim, length check)
  │   ├─ ChatRepository.createMessage()
  │   └─ ChatPubSub.publish(gameId, message)
  │
  ├─ getGameSnapshot(gameId, limit)
  │   ├─ ChatRepository.getRecentMessages()
  │   └─ ChatRepository.countMessages()
  │
  └─ deleteMessage(messageId)
      └─ ChatRepository.deleteMessage()

────────────────────────────────────────────────────────────────────

┌─────────────────────────────────────────────────────────────────┐
│                    REPOSITORY LAYER (ISP)                        │
└─────────────────────────────────────────────────────────────────┘

ChatRepository (IChatReader + IChatWriter)
  ├─ getRecentMessages(gameId, limit) → GameChatMessage[]
  ├─ countMessages(gameId) → number
  ├─ createMessage(data) → GameChatMessage
  └─ deleteMessage(id) → void

────────────────────────────────────────────────────────────────────

┌─────────────────────────────────────────────────────────────────┐
│                       PUBSUB LAYER                               │
└─────────────────────────────────────────────────────────────────┘

InMemoryChatPubSub (IChatPubSub)
  ├─ publish(gameId, message)
  │   └─ Call all handlers for this gameId
  │
  └─ subscribe(gameId, handler)
      └─ Returns unsubscribe function

  ┌─────────────────────────────────────────┐
  │  Production: RedisChatPubSub            │
  │  - Same interface                       │
  │  - Multi-replica support                │
  │  - Just swap implementation             │
  └─────────────────────────────────────────┘

────────────────────────────────────────────────────────────────────

┌─────────────────────────────────────────────────────────────────┐
│                       DATA LAYER                                 │
└─────────────────────────────────────────────────────────────────┘

ViewerIdentity
  ├─ id (UUID, PK)
  ├─ email (unique)
  ├─ firstName (NEW)
  ├─ lastName (NEW)
  └─ chatMessages → GameChatMessage[]

GameChatMessage
  ├─ id (UUID, PK)
  ├─ gameId (UUID, FK → Game.id, CASCADE DELETE)
  ├─ viewerId (UUID, FK → ViewerIdentity.id, CASCADE DELETE)
  ├─ displayName (string, "First L.")
  ├─ message (string, 1-240 chars)
  ├─ createdAt (timestamp)
  └─ INDEX (gameId, createdAt DESC) ← Fast newest-first queries

Game
  └─ chatMessages → GameChatMessage[]
      ├─ Deleting Game → Deletes all messages
      └─ Privacy-safe: no orphaned messages
```

---

## Security Flow

```
┌─────────────────────────────────────────────────────────────────┐
│                    JWT TOKEN FLOW                                │
└─────────────────────────────────────────────────────────────────┘

1. Viewer unlocks with email/name
   ↓
2. Server generates JWT with claims:
   {
     viewerId: "uuid",
     gameId: "uuid",          ← SCOPED TO GAME
     slug: "stream-name",
     displayName: "First L.", ← PRIVACY-FRIENDLY
     exp: timestamp + 24h     ← AUTO-EXPIRE
   }
   ↓
3. Frontend stores in localStorage
   ↓
4. All chat requests include JWT
   ↓
5. Middleware validates:
   - Signature valid?
   - Not expired?
   - gameId matches route?
   ↓
6. Request proceeds OR 401 Unauthorized
```

---

## Real-Time Flow

```
┌─────────────────────────────────────────────────────────────────┐
│              SSE (Server-Sent Events) FLOW                       │
└─────────────────────────────────────────────────────────────────┘

VIEWER A                    SERVER                     VIEWER B
   │                           │                           │
   │ GET /chat/stream         │                           │
   ├──────────────────────────►│                           │
   │                           │ Send snapshot (50 msgs)   │
   │◄──────────────────────────┤                           │
   │                           │ Subscribe to pubsub       │
   │                           │                           │
   │                           │         GET /chat/stream  │
   │                           │◄──────────────────────────┤
   │                           │ Send snapshot (50 msgs)   │
   │                           ├──────────────────────────►│
   │                           │ Subscribe to pubsub       │
   │                           │                           │
   │ POST /chat/messages      │                           │
   │ { message: "Hello!" }    │                           │
   ├──────────────────────────►│                           │
   │                           │ Save to DB                │
   │                           │ Publish to pubsub         │
   │◄──────────────────────────┤ (broadcasts to all)       │
   │ (own message echoed)      │                           │
   │                           ├──────────────────────────►│
   │                           │ (message delivered)       │
   │                           │                           │
   │ ← event: chat_message    │    event: chat_message → │
   │   data: {...}             │    data: {...}           │
```

---

## Integration Points

```
┌─────────────────────────────────────────────────────────────────┐
│                  WHERE TO INTEGRATE                              │
└─────────────────────────────────────────────────────────────────┘

BOOTSTRAP ENDPOINTS (must return gameId):
  ├─ GET /api/direct/:slug/bootstrap
  ├─ GET /api/public/watch-links/:org/:team
  ├─ GET /api/public/games/:gameId
  └─ Response: { ..., gameId: "uuid", chatEnabled: true }

STREAM PAGES (add chat sidebar):
  ├─ /watch/[org]/[team]/[[...code]]/page.tsx
  ├─ /direct/[slug]/page.tsx
  ├─ /direct/tchs/page.tsx
  ├─ /direct/tchs/[date]/[team]/page.tsx
  └─ /game/[gameId]/page.tsx

INTEGRATION CODE (5 lines):
  const viewer = useViewerIdentity({ gameId });
  const chat = useGameChat({ gameId, viewerToken: viewer.token });
  
  return viewer.isUnlocked
    ? <GameChatPanel chat={chat} />
    : <ViewerUnlockForm onUnlock={viewer.unlock} />;
```

---

## Data Flow Diagram

```
┌────────────┐    unlock    ┌────────────┐   stores   ┌────────────┐
│   Viewer   │─────────────►│ localStorage│◄───────────│   Token    │
│   (User)   │              └────────────┘            │   (JWT)    │
└──────┬─────┘                                         └──────┬─────┘
       │                                                      │
       │ sends message                                       │ validates
       ▼                                                      ▼
┌────────────┐  validates  ┌────────────┐   reads    ┌────────────┐
│  Message   │────────────►│   Server   │◄───────────│  Database  │
│  (1-240)   │             │  (Express) │            │  (Prisma)  │
└────────────┘             └──────┬─────┘            └────────────┘
                                  │
                          broadcasts via
                                  │
                                  ▼
                           ┌────────────┐
                           │   PubSub   │
                           │ (in-memory)│
                           └──────┬─────┘
                                  │
                          delivers to
                                  │
                    ┌─────────────┴─────────────┐
                    ▼                           ▼
             ┌────────────┐              ┌────────────┐
             │  Viewer A  │              │  Viewer B  │
             │    SSE     │              │    SSE     │
             └────────────┘              └────────────┘
```

---

## File Structure

```
fieldview.live/
├── apps/
│   ├── api/
│   │   ├── src/
│   │   │   ├── lib/
│   │   │   │   ├── viewer-jwt.ts          (JWT generation)
│   │   │   │   └── chat-pubsub.ts         (PubSub abstraction)
│   │   │   ├── middleware/
│   │   │   │   └── viewer-auth.ts         (requireViewerAuth)
│   │   │   ├── repositories/
│   │   │   │   ├── IChatRepository.ts     (interfaces)
│   │   │   │   └── implementations/
│   │   │   │       └── ChatRepository.ts  (Prisma)
│   │   │   ├── services/
│   │   │   │   ├── IChatService.ts        (interface)
│   │   │   │   └── ChatService.ts         (business logic)
│   │   │   └── routes/
│   │   │       ├── public.game-viewer.ts  (unlock)
│   │   │       └── public.game-chat.ts    (SSE + messages)
│   │   └── __tests__/
│   │       ├── unit/
│   │       │   ├── lib/viewer-jwt.test.ts
│   │       │   └── repositories/ChatRepository.test.ts
│   │       └── live/
│   │           └── public.game-viewer.test.ts
│   │
│   └── web/
│       ├── hooks/
│       │   ├── useGameChat.ts             (SSE connection)
│       │   └── useViewerIdentity.ts       (unlock state)
│       └── components/
│           ├── GameChatPanel.tsx          (full UI)
│           └── ViewerUnlockForm.tsx       (identity form)
│
└── packages/
    └── data-model/
        └── prisma/
            ├── schema.prisma              (ViewerIdentity, GameChatMessage)
            └── migrations/
                └── 20260106041438_add_viewer_names_and_game_chat/
```

---

## Summary

- ✅ **Universal**: Works with any stream type
- ✅ **Portable**: Zero coupling, plug-and-play
- ✅ **Scalable**: Redis-ready, indexed queries
- ✅ **Secure**: JWT-scoped, privacy-friendly
- ✅ **Tested**: 19 tests passing
- ✅ **Documented**: 4 comprehensive guides

**Ready for production rollout!** 🚀

ROLE: engineer STRICT=false


components:
  parameters:
    GameId:
      name: gameId
      in: path
      required: true
      schema:
        type: string
        format: uuid
      description: Game identifier

    PurchaseId:
      name: purchaseId
      in: path
      required: true
      schema:
        type: string
        format: uuid
      description: Purchase identifier

    OwnerId:
      name: ownerId
      in: path
      required: true
      schema:
        type: string
        format: uuid
      description: Owner account identifier

    EntitlementToken:
      name: token
      in: path
      required: true
      schema:
        type: string
      description: Entitlement token (signed, expiring)

    SessionId:
      name: sessionId
      in: path
      required: true
      schema:
        type: string
        format: uuid
      description: Playback session identifier

    SessionIdQuery:
      name: sessionId
      in: query
      required: true
      schema:
        type: string
        format: uuid
      description: Playback session identifier (query parameter)

    Page:
      name: page
      in: query
      schema:
        type: integer
        minimum: 1
        default: 1
      description: Page number for pagination

    Limit:
      name: limit
      in: query
      schema:
        type: integer
        minimum: 1
        maximum: 100
        default: 50
      description: Items per page
